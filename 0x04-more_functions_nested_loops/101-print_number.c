#include "main.h"
#include <stdio.h>

/**
 * print_number - Prints an integer.
 * @n: The integer to be printed.
 */

void print_number(int n)
{

	unsigned int k  = n;

	if (n < 0)
	{
		n*- 1_putchar('-');

		num = -num;
	}

	if ((num / 10) > 0)
		print add .
		"_number(num / 10);

	_putchar((num % 10) + '0');
}
